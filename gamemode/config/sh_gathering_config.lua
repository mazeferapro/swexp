-- ============================================================
-- Star Wars: Expedition — Конфигурация системы добычи
-- config/sh_gathering_config.lua
--
-- Настройки полевой добычи материалов (swexp_material_node).
-- Дополнительные параметры самих узлов определены прямо
-- в entities/swexp_material_node/init.lua.
-- ============================================================

SWExp.GatheringConfig = SWExp.GatheringConfig or {}

-- ============================================================
-- ПАРАМЕТРЫ ДОБЫЧИ (используются sv_gathering.lua)
-- ============================================================

-- Максимальная дистанция от игрока до узла (в единицах движка)
SWExp.GatheringConfig.GatherRange    = 120

-- Время удержания E для завершения добычи (в секундах)
SWExp.GatheringConfig.GatherDuration = 3.5

-- Кулдаун после успешной добычи (в секундах)
SWExp.GatheringConfig.GatherCooldown = 4.0

-- Как часто синхронизировать прогресс клиенту (в секундах)
SWExp.GatheringConfig.SyncInterval   = 0.12

-- ============================================================
-- ПАРАМЕТРЫ УЗЛОВ ДОБЫЧИ
-- ============================================================

-- Диапазон случайных зарядов (сколько раз можно добыть с одного узла)
SWExp.GatheringConfig.ChargesMin = 1
SWExp.GatheringConfig.ChargesMax = 3

-- Пауза перед удалением истощённого узла (в секундах)
SWExp.GatheringConfig.DepletedRemoveDelay = 1.5

-- ============================================================
-- ТИПЫ РЕСУРСОВ
-- (описание для будущих расширений / других систем)
-- Основные типы узлов описаны в entities/swexp_material_node/init.lua
-- ============================================================

--[[
    Все типы узлов возвращают item "mat_basic" в инвентарь игрока.
    Чтобы добавить новый тип ресурса с отдельным item:
      1. Зарегистрируйте новый item в sh_inventory.lua
      2. Добавьте тип в таблицу MATERIAL_TYPES в init.lua узла
         с полем itemID = "ваш_новый_item_id"
]]

print("[SWExp] Конфиг добычи материалов загружен.")
