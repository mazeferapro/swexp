-- ============================================================
-- Star Wars: Expedition — Конфигурация гранатных слотов
-- modules/grenade/sh_grenade_config.lua
--
-- 3 слота под гранаты, активный выбирается одной кнопкой-циклером,
-- бросок — отдельной кнопкой. Биндинг настраивается в F4 →
-- Настройки → Гранаты.
-- ============================================================

SWExp.Grenade = SWExp.Grenade or {}

SWExp.Grenade.Config = {
    -- Кулдаун между бросками (секунды)
    ThrowCooldown = 1.5,

    -- Максимальное время от выдачи SWEP до автоматического стрипа
    -- (на случай если SWEP не успел кинуть)
    SWEPMaxLifetime = 4.0,

    -- Сколько слотов под гранаты
    SlotCount = 3,

    -- Звук при попытке броска без гранаты
    EmptyClickSound = "buttons/button10.wav",
}

print("[SWExp][Grenade] sh_grenade_config.lua загружен")
